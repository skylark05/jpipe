pipelines:
  - name: TestBash
    configuration:
      environmentVariables:
        pipe: true
    steps:
      - name: run
        type: Bash
        configuration:
          environmentVariables:
            foo: bar
            fizz: bin
        execution:
          onExecute:
            - echo foo is $foo and fizz is $fizz
